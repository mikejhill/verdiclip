# Icons

Icons are currently **generated programmatically** at build time (see `src/verdiclip/` for icon generation logic).

These directories are reserved for future custom icon files (e.g., `verdiclip.ico`, tool-specific PNGs). If custom icons are added here, the build scripts and application code should be updated to reference them instead of generating icons at runtime.
